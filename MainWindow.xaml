<Window x:Class="PolygonEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PolygonEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="LightBlue">
            
        </Grid>
        <Canvas Name="canvas" Grid.Column="1" Background="LightGray"
                VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Image Name="canvasImage" Source="{Binding PolygonBitmap, Mode=TwoWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Canvas>
    </Grid>
</Window>
